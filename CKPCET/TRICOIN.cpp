#include <iostream>
#include <algorithm>
#include <math.h>
using namespace std;
int main()
{
    int T;
    cin >> T;
    while (T--)

    {
        long long N;
        cin >> N;
        long long i = 1;
        long long tri__height = (-1 + (sqrt(1 + 4 * N * 2))) / 2;

        cout << tri__height << endl;
    }
}